Project Preview
Dog breeds classification with tensorflow keras.
Download the zip file and unzip it then upload to colab.
Create a folder under root directory named images.
.ipynb_checkpoints in the images fold can be removed in terminal:
!pip install kora
from kora import console
console.start()
use ls -a to check the file, rmdir to remove .ipynb_checkpoints directory.
